# Git Authorization

Use Git only when the user explicitly requests it or the active workflow
requires it. Once authorized, run the Git operations needed to achieve the
authorized objective and keep them within its scope.
