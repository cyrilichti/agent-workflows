---
alwaysApply: true
---

# Project AI Context

This project uses `.agents/` as its structured AI context directory.

At the beginning of every new Agent conversation, before responding to the
first developer message, read and execute:

- `AGENTS.md`
