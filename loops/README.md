# Loops

This directory contains reusable execution loops installed under
`.agents/loops/`.

Each loop defines one iterative strategy with measurable validation,
continuation rules, stopping conditions, and escalation behavior.

Loops control repetition toward a goal. Complete process orchestration belongs
in `../workflows/`.
