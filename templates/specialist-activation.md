# Specialist Activation

Use when reporting an activated specialist profile.

## Format

```markdown
> **<readable agent name>** — <short reason related to the task>
```

## Rules

- Convert the profile identifier to a readable name.
- Output exactly one inline Markdown blockquote containing the readable name
  and one short reason related to the task.
- Do not include metadata, alternatives, internal reasoning, or activity logs.
